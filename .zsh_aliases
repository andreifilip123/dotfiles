alias zshconfig="vi ~/.zshrc"
alias ohmyzsh="vi ~/.oh-my-zsh"
alias ali="vi ~/.oh-my-zsh/custom/custom.zsh"

# MacOS disable hold for special characters
alias disableHold='defaults write -g ApplePressAndHoldEnabled -bool false'
alias enableHold='defaults write -g ApplePressAndHoldEnabled -bool true'

# MacOS show hidden files
alias showFiles='defaults write com.apple.finder AppleShowAllFiles YES; killall Finder /System/Library/CoreServices/Finder.app'
alias hideFiles='defaults write com.apple.finder AppleShowAllFiles NO; killall Finder /System/Library/CoreServices/Finder.app'

# Remove all node_modules directories from folder
alias clear_node_modules="find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +"

# React native aliases
alias rn='react-native'
alias rnri='rn run-ios'
alias rnris='rnri --simulator "iPhone SE"'
alias rnrisr='rnris --configuration Release'
alias rnrid='rnri --device dev-phone'
alias rnridr='rnrid --configuration Release'

alias rnra='rn run-android'
alias rnrar='rnra --variant release'

alias cleanrn1='rm -rf node_modules; cd ios; rm -rf Pods build; cd ../; yarn; yarn postinstall-with-wallet-data; cd ios; pod install; cd ../; watchman     watch-del-all; echo "\n\n\nNow open xcode and build from there. After it fails, come back here and run cleanrn2"'
alias cleanrn2='cd node_modules/react-native/third-party/glog-0.3.5;./configure;make;make install; cd ../../../../'
alias cleanrn="rm -rf node_modules; cd ios/; rm -rf Pods build; cd ..; yarn; yarn postinstall-with-wallet-data; cd ios/; pod install; cd ..; cd android; ./gradlew clean; cd ..;"
alias rnclean="rm -rf node_modules; cd ios/; rm -rf Pods build; cd ..; yarn; yarn postinstall-with-wallet-data; cd ios/; pod install; cd ..; cd android; ./gradlew clean; cd ..;"
alias pods='cd ios; rm -rf Pods build; pod install; cd ../'
alias devices='instruments -s devices'

# Aliases for git
alias git="hub"
alias g="git"
alias gs="git status"
alias goto="git checkout"
alias glog="git log --pretty=oneline"
alias gc="git commit"
alias gd="git diff"
alias gds="git diff --cached"
alias gaa="git add -A"
alias gaas='gaa;g stash;'
alias ga="git add"
alias gp="git push"
alias gf="git fetch"
alias gl="git pull"
alias glr="git pull --rebase"
alias gsta="git stash"
alias gstp="git stash pop"
alias brename="git branch -m"
alias bdel="git branch -D"
gpeek() {
  git diff $1^ $1
}
greset() {
  if [ "$#" -eq 2 ]; then
    case $1 in
        "soft"*)
          g reset HEAD $2
          ;;
        "hard"*)
          g checkout -- $2
          ;;
        *)
          echo "First argument must be one of 'soft', 'hard'"
          ;;
    esac
  fi
  if [ "$#" -eq 1 ]; then
    case $1 in
        "soft"*)
          g reset
          ;;
        "hard"*)
          g reset --hard HEAD
          ;;
        "-h"*)
            echo "usage: greset [soft|hard <file>][<file>][-h]"
            echo ""
            echo "examples:"
            echo "greset soft file.txt <-- unstages file"
            echo "greset hard file.txt <-- discards changes to file"
            echo ""
            echo "greset soft <-- unstages everything"
            echo "greset hard <-- discards all local changes"
            echo "greset file.txt <-- discards changes to file"
            echo ""
            echo "Report bugs to: andreifilip123@gmail.com"
          ;;
        *)
          g checkout -- $1
          ;;
    esac
  fi
}

# Autogenerated aliases
alias chrome='/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome'
alias vi="vim"
alias pa="php artisan"

alias vim='/usr/local/Cellar/vim/8.1.1100/bin/vim'
